services:
  remus-eth-usdc:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - /home/chepe/.starkli:/keystores
    restart: unless-stopped
    command: ["--cfg", "./strats/remus_eth_usdc.toml"]

  remus-strk-usdc:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - /home/chepe/.starkli:/keystores
    restart: unless-stopped
    command: ["--cfg", "./strats/remus_strk_usdc.toml"]

  remus-wbtc-usdc:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - /home/chepe/.starkli:/keystores
    restart: unless-stopped
    command: ["--cfg", "./strats/remus_wbtc_usdc.toml"]

  remus-wbtc-dog:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    volumes:
      - /home/chepe/.starkli:/keystores
    restart: unless-stopped
    command: ["--cfg", "./strats/remus_wbtc_dog.toml"]